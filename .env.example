# Environment Variables Example for Local Development
# Copy this file to .env and update values as needed

# Authorization Service Configuration
# These are optional - default values are set in application-dev.properties
AUTHORIZATION_ENDPOINT=http://localhost:8081
SERVICE_API_KEY=dev-api-key

# Note: Database configuration is already set in application-dev.properties
# If you need to override the database settings, you can set:
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/inventory_db
# SPRING_DATASOURCE_USERNAME=nicholaspark
# SPRING_DATASOURCE_PASSWORD=
